name: Daily Task Recommendations

on:
  schedule:
    # Run every day at 9 AM KST (0 AM UTC)
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  recommend-tasks:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run Task Recommender
        id: recommender
        run: |
          node scripts/task-recommender.js
          echo "report_file=DAILY_REPORT.docx" >> $GITHUB_OUTPUT

      - name: Commit Report
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add *.docx package.json package-lock.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "📋 Update daily task report - $(date +%Y-%m-%d)"
          git push || echo "Nothing to push"

      - name: Send Email Report
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "PPEL Lab 일일 보고서"
          to: smlim@jbnu.ac.kr
          from: PPEL Lab Bot <${{ secrets.SMTP_USERNAME }}>
          body: |
            안녕하세요 임수만 교수님,

            오늘의 PPEL Lab 일일 보고서를 첨부파일(Word)로 보내드립니다.

            - 최신 연구동향 (arXiv 논문)
            - 연구 프로그램 추천
            - 웹사이트 개선 작업

            감사합니다.
            PPEL Lab 자동화 시스템
          content_type: text/plain
          attachments: DAILY_REPORT.docx
        continue-on-error: true
